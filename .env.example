# Copy to .env locally; use Hugging Face Space secrets for deployment.

OPENAI_API_KEY=
# Optional — defaults to cost-efficient tier.
LLM_MODEL=gpt-4o-mini

# Streamable HTTP MCP (Meridian ordering backend).
MCP_SERVER_URL=https://order-mcp-74afyau24q-uc.a.run.app/mcp

# Optional Azure OpenAI–style endpoints.
# OPENAI_BASE_URL=

# OPENAI_TIMEOUT_SECONDS handled via REQUEST_TIMEOUT_SECONDS in Settings — see settings.py field names.
